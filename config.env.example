# 复制为 config.env 后修改。config.env 不应提交到版本库。
ROS_DISTRO=jazzy
BUILD_TYPE=Release
COLCON_WORKERS=0
AUTO_SOURCE_BASHRC=0

# 企业代理示例（不用时保持注释）
# http_proxy=http://proxy.example.com:8080
# https_proxy=http://proxy.example.com:8080
# no_proxy=localhost,127.0.0.1,192.168.0.0/16
